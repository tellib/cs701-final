export type numdleLog = {
    guess: string,
    perfect: number,
    imperfect: number
}

export type numdleLeaderBoardData = {
    userName: string | null,
    clearTime: Date | null,
    attempts: number,
}